function sum(a,b) {
    c = a + b;
    console.log(c);
};
console.dir(sum)
const a = 5;
const b = 6;

sum(a,b);

// функции могут быть именованными, присвоены переменным, 
// анонимными, аргументами при вызове другой функции
// функция это объект, значит она имеет свойства тоже

