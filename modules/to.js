import {PI, getArea, getCircumference, getVolume} from './from.js';

console.log(PI);
const circumference = getCircumference(10);
const area = getArea(10)
const volume = getVolume(10)

console.log(circumference, area, volume)
