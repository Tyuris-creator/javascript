const myName = 'Bogdan'

const sum = (a, b) => {return a + b} 

export default sum



// если много то export {one, two} в другом файле import {one as o, two} from './...%+.mjs'


// es6 modules an external file that contains reusable code that
// can be imported into other javascript files.
// write reusable code for many different apps
// can contain variables, classes, functions and more
// introduced as part ECMAScript 2015 update